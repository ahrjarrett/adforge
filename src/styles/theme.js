const offWhite = `249,245,242`
const background = `rgba(${offWhite}, 0.4)`
const primaryRaw = `239, 64, 34`
const primary = `rgba(${primaryRaw}, 1)`
const greyRaw = `88, 88, 91`
const grey = `rgba(${greyRaw}, 1)`
const lightGrey = `rgba(160,146,133,0.81)`
const highlight = `#fff13a`
export const theme = {
  offWhite,
  background,
  primary,
  primaryRaw,
  grey,
  lightGrey,
  greyRaw,
  highlight,
}
